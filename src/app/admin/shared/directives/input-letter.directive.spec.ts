import { InputLetterDirective } from './input-letter.directive';

describe('InputLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new InputLetterDirective();
    expect(directive).toBeTruthy();
  });
});
